dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {url 'https://meme-api.herokuapp.com/gimme'}
    }
}
rootProject.name = "MemeShare"
include ':app'
